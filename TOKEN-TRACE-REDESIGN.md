# Token Trace 可运行版重构说明

更新时间：2026-08-17

## 目标

将现有 Token 数字面板升级为两层体验：

- 悬浮小卡片：只显示当前对话的本轮趋势、累计、上下文和缓存效率。
- 大型界面：显示今日累计、任务占比排行、规则化原因与建议、今日模型消费对比、价格编辑和 Agent 分析包导出。

## 数据契约

`token-stats.mjs` 保留现有当前对话字段，并新增 `daily`：

- 今日输入、缓存、输出、总量、请求数和任务数；
- 昨日同比、近 7 日均值和 24 小时分桶；
- 今日任务列表，按 Token 总量降序；
- 每项任务只提供统计摘要、最多三条可解释规则信号和对应动作。

大型界面的模型价格保存在页面本地存储中。统计程序不抓取网络价格，也不把价格硬编码为事实；内置值只作为可编辑的演示目录。

## 规则分析

首版只使用可由 Token 日志验证的规则：

- 上下文连续增长；
- 最近缓存命中率明显低于任务均值；
- 单轮消耗显著高于任务中位数；
- 输出占比偏高；
- 请求次数或连续请求密度偏高。

规则结果必须包含证据文本，并使用“可能”“建议”等措辞，不把相关性表述为确定因果。

## 隐私与导出

- CDP 载荷不包含绝对日志路径；
- 今日任务可在本机界面显示短标题，但 Agent 分析包默认不导出对话正文或绝对路径；
- 导出为 Markdown，包含结构化统计、规则证据、当前价格假设和预设分析提示词；
- 导出只由用户点击触发，不自动发送给任何服务。

## 本次实现边界

- 保留 Codex++ 注入方式、当前对话 ID 检测、拖动、收起和位置记忆；
- 不主动重新打包 exe；
- 源码通过验证后，同步面板脚本到已存在的 Codex++ `user_scripts` 目录，并在发现正在运行的 Node 监控时安全重启该进程。

## 下一阶段：去除 Codex++（已确认，尚未实现）

用户已明确要求后续版本不再依赖 Codex++，也不再尝试给 Codex 增加侧边栏。已确认的产品方向：

- 后台程序登录 Windows 后静默等待，检测到 Codex 进程后自动开始统计；
- 小界面改为独立的 Windows 无边框悬浮入口，吸附并跟随 Codex 主窗口右侧；
- 鼠标悬停时展开 Token 趋势卡，移开后延迟收起；
- 点击悬浮卡，在浏览器中打开本机大型统计页面；
- 后台程序直接读取 rollout 日志并向本机网页提供数据，不使用 CDP、页面注入或端口 9229；
- 无法从日志可靠判断“刚切换但尚未产生请求的旧任务”，因此界面应使用“最近活动任务”措辞，并提供任务选择或固定能力。

待确认：Codex 最大化且右侧没有外部空间时，悬浮入口应缩进窗口内部、移动到左侧，还是暂时隐藏。
