# Token Trace 可运行版重构说明

更新时间：2026-08-18

## 目标

将现有 Token 数字面板升级为两层体验：

- 独立 Windows 悬浮入口：自由拖动圆形图片悬浮球，默认黑猫 Token logo，点击展开/收起，显示最近活动任务、本轮趋势、今日累计和缓存命中。
- 浏览器大型界面：显示今日累计、任务占比排行、Token 构成、今日模型消费对比、价格编辑和提示词+数据包复制。

## 数据契约

`token-stats.mjs` 保留现有当前对话字段，并新增 `daily`：

- 今日输入、缓存、输出、总量、请求数和任务数；
- 昨日同比、近 7 日均值和 24 小时分桶；
- 今日任务列表，按 Token 总量降序；
- 每项任务只提供统计摘要、最多三条可解释规则信号和对应动作。

大型界面的模型价格保存在页面本地存储中。统计程序不抓取网络价格，也不把价格硬编码为事实；内置值只作为可编辑的演示目录。

## 分析策略

新版不在产品内展示本地“异常/原因/建议”板块。统计程序可以保留内部规则函数供兼容或未来实验，但本机服务对外的 `/api/state` 不返回规则结果，浏览器界面也不渲染规则诊断。

核心策略是：产品负责把 Token 数据整理干净，用户点击复制“提示词 + 数据包”后，交给任意 AI 做进一步分析。

## 隐私与导出

- CDP 载荷不包含绝对日志路径；
- 今日任务可在本机界面显示短标题或首条用户消息摘要；
- 数据包包含任务摘要、结构化统计、当前价格假设和预设分析提示词；
- 数据包不包含完整对话正文、绝对路径、密钥或原始日志；
- 导出只由用户点击触发，不自动发送给任何服务。

## 本次实现边界

- 新版默认不依赖 Codex++，不使用页面注入、侧边栏或调试端口；
- 保留旧注入脚本仅用于兼容，不作为默认路线；
- 不主动重新打包 exe；
- 源码通过验证后，不主动安装自启、不主动打包、不主动提交。

## 去除 Codex++ 的伴生程序方向

用户已明确要求后续版本不再依赖 Codex++，也不再尝试给 Codex 增加侧边栏。已确认的产品方向：

- 后台程序登录 Windows 后静默等待，检测到 Codex 进程后自动开始统计；
- 小界面改为独立的 Windows 无边框悬浮入口，可自由拖动并记住位置；
- 点击悬浮球展开 Token 趋势卡，再次点击空白区域可收起；
- 点击悬浮卡，在浏览器中打开本机大型统计页面；
- 后台程序直接读取 rollout 日志并向本机网页提供数据，不使用 CDP、页面注入或端口 9229；
- 无法从日志可靠判断“刚切换但尚未产生请求的旧任务”，因此界面使用“最近活动任务”措辞；
- 悬浮球不再依赖 Codex 窗口边缘，避免悬停时跳动；
- 悬浮球支持用户上传图片，自动居中裁切为圆形封面并本机记忆；
- 悬浮卡保持极简，只展示本轮趋势、今日累计、缓存命中、打开大界面和复制分析包；
- 大型界面移除本地“原因/异常/建议”模块，路径改为：今日累计、任务占比、Token 构成、模型费用对比、复制提示词+数据包；
- 一键复制包包含今日全部任务汇总、任务占比、Token 构成和模型价格表；
- 数据包包含任务标题或首条用户消息摘要，但不包含完整对话、文件路径、密钥或原始日志；
- 不做隐私模式开关，界面直接说明“包含任务摘要，不含完整对话”。
