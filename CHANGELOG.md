# 更新记录（Changelog）

版本更新说明。发布新版本时请在本文件顶部追加最新版本记录。

## v1.1.1（2026-08-20）

### 悬浮球边缘收纳
- 将收纳状态改为真正独立的 `36 × 52` 边缘箭头标签：黑猫悬浮球会完全隐藏，不再露出球体或封面。
- 收纳箭头本身支持沿屏幕边缘拖动，松开后自动吸附到最近的左侧或右侧；轻点箭头恢复完整悬浮球。
- 使用 WPF 原生 `DragMove()` 计算拖动位置，保持高 DPI / 多显示器环境下箭头与鼠标同步。
- 收纳状态、所在边缘与纵向位置继续保存到本机，重新启动后可恢复。

### 文档与发布
- 更新中英文 README、Agent 指南和项目交接说明，明确完整悬浮球与箭头收纳态的交互。
- 一键安装器和便携包默认版本更新为 `v1.1.1`。

## v1.1.0（2026-08-19）

### 大型界面
- 移除重复顶部导航，将总览、阶段对比、模型价格、数据包、主题、悬浮封面和复制分析包统一收纳到左侧栏。
- 新增今日 / 近 7 天 / 近 30 天三档阶段对比；今日使用昨日同期，7 天和 30 天使用前一等长周期作为基准。
- 对比页同时展示总 Token、日均 Token、单任务平均 Token、任务数量，以及新输入、缓存输入和输出变化。
- 修复浅色主题继承颜色和缺少历史基准时的渲染问题；无基准数据会明确显示“暂无基准”。

### 悬浮球
- 改用 WPF 原生拖窗，修复高 DPI / 多显示器环境下悬浮球逐渐偏离鼠标的问题。
- 使用真正的圆形裁切和高质量图片缩放，修复方形窗口及边缘毛刺。
- 鼠标悬停时从球的侧边展开独立快速卡片，移出球和卡片后自动收起。
- 拖到左右屏幕边缘后自动吸附并显示箭头，可将球收纳到边缘或再次恢复。
- 修复 Windows PowerShell 5.1 下圆形裁切初始化失败导致悬浮窗无法显示的问题。

### 文档与发布
- 更新中英文 README 和 Agent 指南，加入新版真实 demo 截图及阶段对比说明。
- 一键安装器和便携包默认版本更新为 `v1.1.0`。

## v1.0.0（2026-08-18）

### 开源一键安装
- 安装器改为固定发布标签 `v1.0.0`，优先 jsDelivr CDN、失败时回退 GitHub Raw；彻底移除 GitHub API、GitHub 登录和 Token 依赖。
- 新增带 Node.js 运行时的便携 ZIP 构建脚本；用户可解压后双击 `Install.cmd`。
- 移除旧 Codex++ 注入脚本、旧启动器及旧预览图，避免新用户进入过期链路。
- 新增 MIT `LICENSE`、安全与第三方运行时说明、公开发布自检脚本。

### WMI 事件守护
- 将守护程序原有的每 2 秒 Codex 进程轮询改为筛选后的 Windows WMI 启动 / 退出事件订阅。
- 收到退出事件后会确认全部 Codex / ChatGPT 进程均已关闭，再停止 Token Trace。
- 卸载守护模式时保留本机主题、模型价格和自定义悬浮封面。

### Token Trace 分析界面
- 大型浏览器界面新增深色 / 浅色主题切换。
- 重做浅色主题视觉：保持与深色主题相同布局位置，仅调整为浅灰画布、白色圆角卡片和紫色主视觉。
- 悬浮卡支持跟随大型面板深色 / 浅色主题切换，并将展开卡片改为圆角窗口。
- 悬浮球默认使用黑猫 Token logo；浏览器大型面板支持用户上传图片后自动圆形裁切为自定义封面。
- 悬浮面板改为独立 Windows 悬浮入口，可自由拖动并记住位置，点击展开/收起。
- 新增浏览器大型面板：今日累计、任务 Token 占比、Token 构成和模型理论消费对比。
- 新增按今日总 Token 构成计算的模型理论消费对比，以及可在本机修改的演示价格表。
- 新增可复制的“提示词 + 数据包”；包含任务摘要和数字指标，不包含完整对话、绝对日志路径、密钥或原始日志。
- 移除新版大型界面的本地异常、原因和建议板块，分析交给用户复制数据包后询问任意 AI。

### 数据统计
- 新增 `token-stats.mjs --server`，提供 `GET /`、`GET /api/state`、`GET /api/pack` 和模型价格接口。
- 新增按本机日期聚合的今日任务、24 小时分桶、昨日对比和近 7 日均值。
- `/api/state` 不返回规则 `insights`，避免新版界面依赖本地诊断。
- 修复占位新任务兜底检测中未定义调试端口变量的问题。

### 无 Codex++ 伴生模式
- 新增低耗启动器模式：桌面快捷方式启动 Codex 时同步启动 Token Trace，Codex 退出后同步关闭，无需登录后常驻守护进程。
- 新增 `dashboard.html`、`floating-panel.ps1` 和 `start-token-trace.ps1`。
- 更新 `guardian.ps1`：检测到 Codex 后启动本机服务和悬浮入口，Codex 退出后停止。
- 新版默认不需要 Codex++，不使用页面注入、侧边栏或端口 9229。

## v1.2（2026-08-14）

### 自启方案重构
- 开机自启改为「计划任务（登录触发）」唯一方式，移除启动文件夹快捷方式方案（旧版遗留快捷方式会在安装时自动清理）。
- 守护进程新增单实例锁，避免重复拉起。

### 可观测性
- 新增守护进程运行日志 `%LOCALAPPDATA%\ccm-token-spend\guardian.log`：记录检测 Codex、启动 / 停止 / 重启监控、失败原因。
- 守护进程的启动 / 停止错误不再静默忽略，统一写入日志，便于排查面板「等待数据」。

### 文档
- README 更新自启安装说明与排查指引。
- 新增本更新记录文件（后续版本在此增量追加）。

## v1.1（2026-08-06）

- 新增开机自启守护进程（当时使用启动文件夹快捷方式）：Codex 启动自动拉起监控、退出自动停止、监控崩溃自动重启。
- 面板：四角缩放、面板与小按钮位置 / 大小记忆、顶部汇总区与底部「更新于」栏常驻、会话历史标题列随宽度拉伸。
- 空白新对话显示 0，不再显示上一个对话的数据；Codex 启动加载期显示 0，加载完成后自动恢复。
- 输入缓存拆分展示：`输入 X（缓存命中 Y，未命中 Z）`。
- 新增英文 README、agent 版安装指南；新增环境测试报告与 Discussion 模板。
- 修复 `--detail` 崩溃、重复小按钮问题。
- exe 版改为内置 Node 运行时（免单独安装 Node.js）。

## v1.0（2026-08-05）

- 首个版本：Codex / ChatGPT 桌面版右下角 Token 消耗统计面板（本轮 / 会话累计 / 请求次数 / 上下文窗口）。
- 监控进程 `token-stats.mjs` 读取 Codex 本地会话日志，通过 CDP（端口 9229）每秒推送数据到页面。
